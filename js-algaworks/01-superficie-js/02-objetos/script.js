var person = {
    name: 'Antonio',
    age: 19,
    sex: 'male'
}

console.log(person); // accessing the entire object
console.log(person.name); // accessing the name of object

person.name = 'Antonio Henrique Machado'

console.log(person);
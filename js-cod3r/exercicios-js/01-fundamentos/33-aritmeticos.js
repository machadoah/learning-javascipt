const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d
const substracao = a - d
const multiplicacao = a * d
const divisao = d / a
const modulo = a % 2 // resto da divisão

console.log(soma, substracao, multiplicacao, divisao, modulo)

